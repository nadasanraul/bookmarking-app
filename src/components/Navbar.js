import React from 'react';

const Navbar = props => (
    <nav className="navbar navbar-dark bg-dark">
        <div className="container">
            <a className="navbar-brand" href="#">Bookmarking Application</a>
        </div>
    </nav>
);

export default Navbar;